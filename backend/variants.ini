[madchess]
parent = chess
# Test showed that standard chess variant handles rank 1 pawns correctly,
# while explicit pawnStartRank=0 causes move generation issues.
# Keeping it simple allows Fairy-Stockfish to use its robust standard chess logic.
